<UserControl x:Class="ChuniPet.Views.UniversePageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="230" d:DesignWidth="510">
    <UserControl.Resources>
        <!-- Modern toggle switch style using a CheckBox -->
        <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Border x:Name="SwitchTrack"
                                    Width="44" Height="22"
                                    CornerRadius="11"
                                    Background="#44FFFFFF">
                                <Border x:Name="SwitchThumb"
                                        Width="18" Height="18"
                                        CornerRadius="9"
                                        Background="White"
                                        HorizontalAlignment="Left"
                                        Margin="2,0,0,0" />
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="SwitchTrack" Property="Background" Value="#2E9BFF" />
                                <Setter TargetName="SwitchThumb" Property="HorizontalAlignment" Value="Right" />
                                <Setter TargetName="SwitchThumb" Property="Margin" Value="0,0,2,0" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SettingRowStyle" TargetType="Grid">
            <Setter Property="Margin" Value="0,0,0,18" />
        </Style>

        <Style x:Key="SettingLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </UserControl.Resources>
    <Grid Margin="8,8,8,8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />   <!-- search box -->
            <RowDefinition Height="*" />      <!-- list -->
            <RowDefinition Height="Auto" />   <!-- back button -->
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Cursor="Hand"
              Margin="0,-30,0,4">
            <TextBlock Grid.Row="0"
                       Text="Settings"
                       FontSize="20" FontWeight="Bold"
                       Foreground="Black"
                       HorizontalAlignment="Left"
                       Margin="20,10,0,10" />
        </Grid>

        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="0,0,0,10" Width="400">

                <!-- Stop Penguin Movement -->
                <Grid Style="{StaticResource SettingRowStyle}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Stop penguin movement"
                               Style="{StaticResource SettingLabelStyle}" />
                    <CheckBox x:Name="StopMovementToggle"
                              Grid.Column="1"
                              Style="{StaticResource ToggleSwitchStyle}"
                              Click="StopMovementToggle_Click" />
                </Grid>

                <!-- Penguin Moves During Startup -->
                <Grid Style="{StaticResource SettingRowStyle}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Penguin moves on startup"
                               Style="{StaticResource SettingLabelStyle}" />
                    <CheckBox x:Name="MoveOnStartupToggle"
                              Grid.Column="1"
                              Style="{StaticResource ToggleSwitchStyle}"
                              Click="MoveOnStartupToggle_Click" />
                </Grid>

                <!-- Run App On Startup -->
                <Grid Style="{StaticResource SettingRowStyle}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Run app on startup"
                               Style="{StaticResource SettingLabelStyle}" />
                    <CheckBox x:Name="RunOnStartupToggle"
                              Grid.Column="1"
                              Style="{StaticResource ToggleSwitchStyle}"
                              Click="RunOnStartupToggle_Click" />
                </Grid>

                <!-- Audio Volume: Slider + Textbox interconnected -->
                <Grid Style="{StaticResource SettingRowStyle}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Audio volume"
                               Style="{StaticResource SettingLabelStyle}"
                               Margin="0,0,0,8" />

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="60" />
                        </Grid.ColumnDefinitions>

                        <Slider x:Name="VolumeSlider"
                                Grid.Column="0"
                                Minimum="0" Maximum="100"
                                VerticalAlignment="Center"
                                Margin="0,0,12,0"
                                ValueChanged="VolumeSlider_ValueChanged" />

                        <TextBox x:Name="VolumeTextBox"
                                 Grid.Column="1"
                                 Height="28"
                                 VerticalContentAlignment="Center"
                                 HorizontalContentAlignment="Center"
                                 TextChanged="VolumeTextBox_TextChanged" />
                    </Grid>
                </Grid>

                <!-- Divider -->
                <Border Height="1" Background="#22FFFFFF" Margin="0,10,0,20" />

                <!-- GitHub Button -->
                <Button x:Name="GitHubButton"
                        Content="View on GitHub"
                        Height="36"
                        HorizontalAlignment="Stretch"
                        Margin="0,0,0,12"
                        Click="GitHubButton_Click" />

                <!-- Exit App Button -->
                <Button x:Name="ExitButton"
                        Content="Exit App"
                        Height="36"
                        HorizontalAlignment="Stretch"
                        Background="#A13544"
                        Foreground="White"
                        Click="ExitButton_Click" />

            </StackPanel>
        </ScrollViewer>

        <Button Grid.Row="2"
                Style="{StaticResource ImageButtonStyle}"
                Click="BackButton_Click"
                HorizontalAlignment="Left"
                Margin="0,6,0,0">
            <Image Source="/Assets/Images/link_level_01.png" Width="40" Height="40" />
        </Button>
    </Grid>
</UserControl>